# STANDARDS.md — 內容與驗證的品質標準

> 適用範圍:`src/content/docs/**`(網站頁面)、`validation/*.py`、`internal/LOOP_LOG.md`。
> 每條標準都寫「為什麼」——規則若不能用一個真實錯誤或省下的時間來辯護,就該刪。
> 可機器檢查的部分由 `python .claude/skills/sealhack-loop/scripts/check.py` 產出計分板。

## S1 數字可追溯
文件中每個實驗數字(CV、私榜、Δ、t 值、門檻)都必須能在 `validation/` 某支腳本的輸出中找到,
腳本名寫在同段或登錄表;改腳本 → 重跑 → 改文件,三件事在同一輪完成。
**為什麼**:2026-08-22 覆現時,C7b 文件寫 -0.010、腳本跑出 -0.0126,差 0.0026 就讓「12/12 通過」變假話。

## S2 主張三件套,判準用相對量與多 seed
每條主張 = 措辭 + L1 文獻 + L2 腳本。腳本的通過判準:
- 用**相對**比較(A 比 B 更接近真相、A 的高估 > B 的高估),不用絕對門檻(|Δ|<0.01 這種)
- 至少 5 個 seed,報「幾個 seed 成立」與效果量範圍,不報單一 seed 的小數點
- **判準必須測主張本身的陳述,不是更嚴格的替身**——加一個主張沒說的閘門(如「seed std 要夠大」)會給假 ❌(第 14、21 輪都栽過)
- 兩個被比較的量必須是**同一種量**(同樣的 OOF 平均方式、同樣的閾值規則),否則差距裡混著評估差異
**為什麼**:C1(絕對→相對)與 C7b(單 seed 絕對門檻換個函式庫版本就翻盤)兩次都栽在這裡;
鐵達尼案例的「集成 -0.043」有一部分是單模@0.5 閾值 vs 集成@搜尋閾值的評估不對等。

## S3 修訂史不刪
被推翻或修訂的主張保留原措辭與修訂原因(登錄表的「修訂史」欄);腳本以版本號或檔內日期註記。
**為什麼**:修訂史是這個站最有說服力的資產——它證明方法論被實驗打過臉而且誠實改了。

## S4 腳本規範
- 第一行 docstring 說明「驗證什麼主張、怎麼驗」;結尾印出結論行(✅/❌ + 關鍵數字)
- `import sys; sys.stdout.reconfigure(encoding="utf-8")` 緊接 docstring(Windows cp950 防呆)
- 固定 seed;只依賴 `validation/requirements.txt`(版本 pin 死);資料只由 `fetch_data.py` 取得,不進 repo
- 單支執行 ≤ 10 分鐘,超過就拆或縮 N;不得訓練同一設定兩次(case_titanic 舊版的雙迴圈);多次切分用 joblib 平行、模型 `n_jobs=1`
- 同一案例的特徵工程只定義一次(模組),各腳本 import 它——兩份定義 = 兩個不可比的數字
**為什麼**:「任何人可重跑」是差異化資產;第一次覆現就被編碼崩潰、缺資料檔、缺 pandas 擋了三次。
small_n_paired.py 與 case_titanic.py 的 base 特徵不同(年齡填補一個用全體中位數、一個用稱謂分組),
導致 §16.3 的 t=2.22/2.83 與案例的結果無法互相印證。

## S5 語言
繁體中文(zh-TW),**code block 與 ASCII 圖內也不得有簡體字**;術語首次出現附英文。
禁用 OpenCC 誤轉詞:引數→參數、超引數→超參數、物件(當 object 用時)→對象、自定義→自訂、演演算法→演算法。
**為什麼**:本站是作者的資歷證明;五問路由與框架總覽是訪客第一眼看到的東西,目前全是簡體。

## S6 頁面結構
- 每個 H2 開頭一句說這節回答什麼問題;可獨立閱讀
- ASCII 圖一律包 ```text 圍欄;表格 ≤ 6 欄(375px 手機要能讀)
- 鐵律/陷阱用 Aside(caution),每頁 ≤ 3 個——超過 3 個等於沒有重點
- 內部連結不得斷(check.py 會掃 src/content/docs 的相對連結)
- 單一真相來源:方法論散文在 src/content/docs、數字在 src/data/*.json、數字的出處在 validation/*.py;三者矛盾時以 validation/ 腳本輸出為準,回頭修 json 與散文
- **內部連結不得斷**:content 的 `/...` 連結必須指向存在的頁面或 validation 檔(check.py 從原始檔稽核,不依賴 build)
**為什麼**:GLOSSARY 的「AutoML 打不贏手調」「只信 CV」與 FRAMEWORK §4.0/§13 直接矛盾,讀者會不知道信哪個。

## S7 簡潔
新增一段文字或一條規則前先問「刪掉什麼換它進來」。一件事只在一處說完;能用實驗數字說明的就不用形容詞。
**為什麼**:使用者要求「優美、簡潔、有效」;方法論越長,「流暢地執行錯誤方向」的機會越多。

## S8 案例標準
一個示範案例必須:六階段都走完 → 用 v2.2 現行規則(不是被廢止的規則)→ 報多次切分的分布而非單次 →
有憑證時附真實 LB → 腳本可重跑。**單次切分的數字不得當結論**。
**為什麼**:鐵達尼「0.8134 vs 0.7948」是 268 人裡差 5 個乘客的單次切分(SE≈0.024),卻成了 §16.6 的教學點。

## S9 外部引用必須真實
所有引用的論文/文章/知名專案,必須以 WebSearch 查證**確實存在**,附可驗證的 URL 或 DOI;作者、年份、標題要對得上。
**寧可不引,也不捏造**——一個假引用會毀掉整站「內容正確性優先」的可信度。
**為什麼**:2026-08-23 使用者要求為每個流程做好文章/專案引用;差異化資產是「可查證」,假引用是最嚴重的自毀。

## 計分板(check.py 自動產出,掃 src/content/docs;規則:不得變差)
| 項目 | 標準 | 目標 |
|---|---|---|
| src/content/docs 簡體字行數 | S5 | 0 |
| OpenCC 誤轉次數 | S5 | 0 |
| validation 腳本可編譯 / UTF-8 防呆 | S4 | 全數 |
| requirements 全 pin、fetch_data.py 存在 | S4 | ✓ |
| 無語言標籤的 code fence 數 | S6 | 0 |
| internal/LOOP_LOG 結構 | 迴圈(已暫停) | ✓ |

## S10 設計系統防漂移(來源:DESIGN.md)
- src/{components,layouts,pages} 的 .astro 檔**禁 raw hex**——顏色一律 `var(--token)`;token 唯一來源是 `src/styles/global.css`(深淺兩主題成對定義)
- 新視覺模式(元件/動效/字體)須先立進 DESIGN.md 再實作;DESIGN.md 與實作不符=缺陷,修其一並在 LOOP_LOG 記一行
- check.py 掃描 raw hex;完整設計驗收清單見 DESIGN.md §八(375px 無橫捲、表格內捲、雙主題對比、40px 觸控目標)
